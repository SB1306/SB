# 🎓 EduVision Pro - คู่มือการ Deploy บน Vercel

## 🚀 วิธี Deploy (แก้ไขแล้ว - พร้อมใช้งาน)

### ปัญหาที่แก้ไขแล้ว ✅

1. ✅ แก้ไข `GoogleGenAI` และ `SchemaType` import ให้ถูกต้อง
2. ✅ เปลี่ยนจาก `process.env` เป็น `import.meta.env.VITE_GEMINI_API_KEY`
3. ✅ แก้ไข model name เป็น `gemini-2.0-flash-exp`
4. ✅ ปรับ `vite.config.ts` ให้เรียบง่ายขึ้น
5. ✅ เพิ่มไฟล์ `vercel.json` และ `.env.example`

---

## 📋 ขั้นตอนการ Deploy

### ขั้นตอนที่ 1: อัปเดตโค้ดใน GitHub

```bash
# 1. คัดลอกไฟล์ที่แก้ไขแล้วทับไฟล์เดิม
# - src/services/geminiService.ts (แก้ไขแล้ว)
# - vite.config.ts (แก้ไขแล้ว)
# - vercel.json (ไฟล์ใหม่)
# - .env.example (ไฟล์ใหม่)

# 2. Commit และ Push
git add .
git commit -m "Fix: Update Gemini API integration for Vercel deployment"
git push origin main
```

### ขั้นตอนที่ 2: ตั้งค่า Environment Variables ใน Vercel

1. ไปที่ https://vercel.com/dashboard
2. เลือกโปรเจค `sb-edu13`
3. ไปที่ **Settings** → **Environment Variables**
4. เพิ่มตัวแปรใหม่:

   **Variable Name:**
   ```
   VITE_GEMINI_API_KEY
   ```

   **Value:**
   ```
   (ใส่ API Key จาก Google AI Studio)
   ```

   **Environments:** เลือกทั้ง 3
   - ✅ Production
   - ✅ Preview
   - ✅ Development

5. คลิก **Save**

### ขั้นตอนที่ 3: ลบตัวแปรเก่า (ถ้ามี)

ถ้ามีตัวแปร `GEMINI_API_KEY` (ไม่มี VITE_) อยู่แล้ว ให้:
1. **ลบตัวแปรเก่าออก** (หรือเปลี่ยนชื่อเป็น `VITE_GEMINI_API_KEY`)
2. หรือเพิ่มตัวแปรใหม่ชื่อ `VITE_GEMINI_API_KEY`

**สำคัญ:** ชื่อตัวแปรต้องขึ้นต้นด้วย `VITE_` เท่านั้น!

### ขั้นตอนที่ 4: Redeploy

1. ไปที่แท็บ **Deployments**
2. คลิก deployment ล่าสุด
3. คลิก **... (3 จุด)** → **Redeploy**
4. รอให้ build เสร็จ (~2-3 นาที)

### ขั้นตอนที่ 5: ทดสอบ

เข้าไปที่: https://sb-edu13.vercel.app/

1. วางลิงก์ YouTube ใดๆ
2. คลิก "เริ่มวิเคราะห์"
3. ถ้า AI วิเคราะห์ได้ = **สำเร็จ! 🎉**

---

## 🔧 การแก้ไขปัญหา

### ปัญหา 1: ยังแสดง "GoogleGenAI is not defined"

**สาเหตุ:** ไฟล์ `geminiService.ts` ยังไม่ได้อัปเดต

**วิธีแก้:**
1. ตรวจสอบว่าไฟล์ `src/services/geminiService.ts` มีบรรทัดนี้:
   ```typescript
   import { GoogleGenAI, SchemaType } from "@google/genai";
   ```
2. และมีการดึง API Key แบบนี้:
   ```typescript
   const apiKey = import.meta.env.VITE_GEMINI_API_KEY || "";
   ```

### ปัญหา 2: API Key ไม่ทำงาน

**สาเหตุ:** Environment Variable ตั้งค่าผิดหรือยังไม่ได้ Redeploy

**วิธีแก้:**
1. ตรวจสอบชื่อตัวแปรว่าเป็น `VITE_GEMINI_API_KEY` (ต้องมี `VITE_` ข้างหน้า)
2. ตรวจสอบว่า API Key ถูกต้อง (ลองสร้างใหม่ใน Google AI Studio)
3. **Redeploy โปรเจค** หลังจากเปลี่ยน Environment Variables

### ปัญหา 3: Build Failed

**สาเหตุ:** Dependencies ขาดหรือไฟล์ config ผิด

**วิธีแก้:**
1. ตรวจสอบ `package.json` มี dependencies ครบ:
   ```json
   "@google/genai": "^1.34.0",
   "lucide-react": "^0.562.0",
   "react": "^19.2.3",
   "react-dom": "^19.2.3"
   ```
2. ลอง **Clear Cache** และ Redeploy ใหม่

### ปัญหา 4: Model not found

**สาเหตุ:** ใช้ชื่อ model ผิด

**วิธีแก้:**
ใน `geminiService.ts` ต้องใช้:
```typescript
const model = "gemini-2.0-flash-exp"; // หรือ "gemini-1.5-flash"
```

---

## 📊 Model ที่แนะนำ

| Model | ความเร็ว | คุณภาพ | ราคา |
|-------|---------|--------|------|
| `gemini-2.0-flash-exp` | เร็วมาก | ดีมาก | ฟรี* |
| `gemini-1.5-flash` | เร็ว | ดี | ฟรี* |
| `gemini-1.5-pro` | ปานกลาง | ดีเยี่ยม | ฟรี* |

*มี Free Quota จำกัด: 15 requests/นาที, 1,500 requests/วัน

---

## 🎯 สรุปไฟล์ที่แก้ไข

1. **src/services/geminiService.ts** - แก้ไข import และ API Key
2. **vite.config.ts** - ลดความซับซ้อน
3. **vercel.json** - เพิ่มไฟล์ config ใหม่
4. **.env.example** - เพิ่มตัวอย่าง

---

## 🆘 ต้องการความช่วยเหลือ?

หากยังมีปัญหา กรุณา:
1. ถ่าย Screenshot ของ Console Error (กด F12 → Console)
2. ส่งข้อความ error มาให้ดู
3. ตรวจสอบ Vercel Build Logs

---

**สร้างโดย:** โรงเรียนบ้านทุ่ม  
**อัปเดต:** มกราคม 2026  
**Powered by:** Google Gemini AI + Vercel
